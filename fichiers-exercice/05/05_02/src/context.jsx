import { createContext } from "react";

const todosContext = createContext()

const todosProvider = (children) => {
    cont [theme, setTheme] = iseState('darck')
    return <todosContext.Provider>{children}</todosContext.Provider>

}